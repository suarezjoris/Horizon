# Pax v4 — Proactivité Passive Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre Pax adaptatif (cooldown basé sur la qualité de réponse) et proactif sur 3 nouveaux déclencheurs software (startup, Forge step, workspace change) via une architecture dual-path avec bannière flottante.

**Architecture:** Le chemin A existant (chat-idle → injection chat) reçoit un timing adaptatif. Un nouveau chemin B (channel mpsc `PaxEvent`) connecte startup/Forge/workspace à une tâche bannière (`run_pax_banner`) qui émet un event frontend non-intrusif.

**Tech Stack:** Rust/Tokio, `notify` crate (déjà en Cargo.toml), Tauri events, vanilla JS.

---

## File Map

| Fichier | Changements |
|---|---|
| `src-tauri/src/pax_daemon.rs` | `ReplyQuality`, `LAST_QUALITY`, `BANNER_LAST_SENT`, `adaptive_cooldown()`, timeout Ignored, `PaxEvent`, `PaxEventSender`, `run_pax_banner()`, watcher workspace |
| `src-tauri/src/main.rs` | channel mpsc, `manage(PaxEventSender)`, spawn `run_pax_banner`, startup trigger 45s, fix `toggle_agent_daemon` |
| `src-tauri/src/forge_daemon.rs` | helper `notify_pax()`, 4 points d'injection ForgeStep |
| `src/index.html` | div `#pax-banner` (ligne 96, après hub-proposal-banner) |
| `src/modules/llm.js` | listener `pax-banner`, inject chat + dismiss |

---

## Task 1: ReplyQuality + adaptive_cooldown() dans pax_daemon.rs

**Files:**
- Modify: `src-tauri/src/pax_daemon.rs:1-36` (imports + statics)
- Modify: `src-tauri/src/pax_daemon.rs:74-96` (`touch_activity`)
- Modify: `src-tauri/src/pax_daemon.rs:134-139` (après `now_secs()`)
- Test: `src-tauri/src/pax_daemon.rs` (bloc `#[cfg(test)]` existant)

- [ ] **Step 1 : Écrire les tests qui échouent**

Ajouter dans le bloc `#[cfg(test)]` existant à la fin de `pax_daemon.rs` :

```rust
#[test]
fn test_adaptive_cooldown_positive() {
    *LAST_QUALITY.lock().unwrap() = ReplyQuality::Positive;
    let c = adaptive_cooldown(0);
    assert!(c >= 180 && c < 210, "Positive cooldown should be ~3-4min, got {}", c);
}

#[test]
fn test_adaptive_cooldown_neutral() {
    *LAST_QUALITY.lock().unwrap() = ReplyQuality::Neutral;
    let c = adaptive_cooldown(0);
    assert!(c >= 540 && c < 600, "Neutral cooldown should be ~9-10min, got {}", c);
}

#[test]
fn test_adaptive_cooldown_ignored() {
    *LAST_QUALITY.lock().unwrap() = ReplyQuality::Ignored;
    let c = adaptive_cooldown(0);
    assert!(c >= 1200 && c < 1380, "Ignored cooldown should be ~20-23min, got {}", c);
}
```

- [ ] **Step 2 : Vérifier que les tests échouent**

```bash
cd src-tauri && cargo test test_adaptive -- --test-threads=1 2>&1 | head -20
```
Attendu : `error[E0425]: cannot find function 'adaptive_cooldown'`

- [ ] **Step 3 : Ajouter le type ReplyQuality et les statics**

Après la ligne 36 (`static AVOIDED_NOTES: ...`), ajouter :

```rust
#[derive(Clone, Copy, PartialEq)]
enum ReplyQuality { Positive, Neutral, Ignored }

static LAST_QUALITY: Lazy<Mutex<ReplyQuality>> =
    Lazy::new(|| Mutex::new(ReplyQuality::Neutral));
```

- [ ] **Step 4 : Ajouter adaptive_cooldown() après now_secs()**

Après la fonction `now_secs()` (actuellement ~ligne 139), ajouter :

```rust
fn adaptive_cooldown(now_secs: u64) -> u64 {
    let jitter = now_secs % 60;
    match *LAST_QUALITY.lock().unwrap() {
        ReplyQuality::Positive => 180 + jitter / 2,
        ReplyQuality::Neutral  => 540 + jitter,
        ReplyQuality::Ignored  => 1200 + jitter * 3,
    }
}
```

- [ ] **Step 5 : Mettre à jour touch_activity() pour détecter la qualité**

Dans `touch_activity()`, remplacer le bloc `if was_waiting` (actuellement lignes 89-95) :

```rust
// AVANT
if was_waiting {
    if is_rejection(msg) {
        avoid_last_note();        // 24h on explicit refusal
    } else {
        avoid_last_note_engaged(); // 2h after normal engagement
    }
}

// APRÈS
if was_waiting {
    if is_rejection(msg) {
        avoid_last_note();
    } else {
        let q = if msg.len() > 60 || msg.contains('?') {
            ReplyQuality::Positive
        } else {
            ReplyQuality::Neutral
        };
        *LAST_QUALITY.lock().unwrap() = q;
        avoid_last_note_engaged();
    }
}
```

- [ ] **Step 6 : Vérifier que les tests passent**

```bash
cd src-tauri && cargo test test_adaptive -- --test-threads=1 2>&1 | tail -10
```
Attendu : `test result: ok. 3 passed`

- [ ] **Step 7 : Commit**

```bash
git add src-tauri/src/pax_daemon.rs
git commit -m "feat(pax): add ReplyQuality detection and adaptive_cooldown()"
```

---

## Task 2: Timeout Ignored + remplacer le cooldown fixe dans run_pax()

**Files:**
- Modify: `src-tauri/src/pax_daemon.rs` (fonction `run_pax()`, ~lignes 310-330)

- [ ] **Step 1 : Remplacer le bloc `if waiting` dans run_pax()**

Dans `run_pax()`, trouver ce bloc (à l'intérieur de `if let Some(last_q) = last_question`) :

```rust
// AVANT
if waiting {
    // Pax asked — wait until user replies (touch_activity clears this).
    continue;
}
```

Remplacer par :

```rust
// APRÈS
if waiting {
    if last_q.elapsed().as_secs() > 900 {
        WAITING_FOR_REPLY.store(false, Ordering::Relaxed);
        *LAST_QUALITY.lock().unwrap() = ReplyQuality::Ignored;
    }
    continue;
}
```

- [ ] **Step 2 : Remplacer le calcul du cooldown fixe**

Dans le même bloc `if let Some(last_q) = last_question`, trouver :

```rust
// AVANT
let required = if msg_count >= MULTI_MSG_THRESHOLD {
    MULTI_MSG_COOLDOWN_SECS.max(QUESTION_COOLDOWN_SECS)
} else {
    QUESTION_COOLDOWN_SECS
};
if since_q < required { continue; }
RECENT_MSG_COUNT.store(0, Ordering::Relaxed);
```

Remplacer par :

```rust
// APRÈS
if since_q < adaptive_cooldown(now) { continue; }
RECENT_MSG_COUNT.store(0, Ordering::Relaxed);
```

- [ ] **Step 3 : Supprimer constantes et variables devenues inutilisées**

Supprimer en haut du fichier (lignes ~13-19) :

```rust
// Supprimer ces trois constantes :
const QUESTION_COOLDOWN_SECS: u64 = 600;
const MULTI_MSG_COOLDOWN_SECS: u64 = 60;
const MULTI_MSG_THRESHOLD: u32 = 2;
```

Garder `IDLE_THRESHOLD_SECS`, `CHECK_INTERVAL_SECS`, `MULTI_MSG_WINDOW_SECS` (encore utilisé dans `touch_activity()`).

Dans la boucle `run_pax()`, supprimer la ligne de chargement devenue inutile (~ligne `let msg_count = ...`) :

```rust
// Supprimer cette ligne dans le corps de la boucle :
let msg_count    = RECENT_MSG_COUNT.load(Ordering::Relaxed);
```

`RECENT_MSG_COUNT.store(0, Ordering::Relaxed)` dans le cooldown check reste — il remet le compteur à zéro proprement après chaque question.

- [ ] **Step 4 : Build pour vérifier l'absence d'erreurs**

```bash
cd src-tauri && cargo build 2>&1 | grep -E "^error" | head -20
```
Attendu : aucune ligne `error`

- [ ] **Step 5 : Commit**

```bash
git add src-tauri/src/pax_daemon.rs
git commit -m "feat(pax): replace fixed 10min cooldown with adaptive timing + Ignored timeout"
```

---

## Task 3: PaxEvent + PaxEventSender + run_pax_banner()

**Files:**
- Modify: `src-tauri/src/pax_daemon.rs` (imports, statics, nouvelles fonctions publiques)

- [ ] **Step 1 : Ajouter l'import Receiver**

En haut du fichier, après `use once_cell::sync::Lazy;` :

```rust
use tokio::sync::mpsc::Receiver;
```

- [ ] **Step 2 : Ajouter le static BANNER_LAST_SENT**

Après `static AVOIDED_NOTES: ...` (et les nouvelles statics de Task 1) :

```rust
static BANNER_LAST_SENT: Lazy<AtomicU64> = Lazy::new(|| AtomicU64::new(0));
```

- [ ] **Step 3 : Ajouter PaxEvent et PaxEventSender**

Après `BANNER_LAST_SENT`, ajouter les deux types publics :

```rust
pub enum PaxEvent {
    Startup,
    ForgeStep { message: String },
    WorkspaceChange,
}

pub struct PaxEventSender(pub tokio::sync::mpsc::Sender<PaxEvent>);
```

- [ ] **Step 4 : Ajouter run_pax_banner()**

Après la fonction `trigger_pax` (en fin de fichier, avant `#[cfg(test)]`) :

```rust
pub async fn run_pax_banner(
    mut rx: Receiver<PaxEvent>,
    tx: tokio::sync::mpsc::Sender<PaxEvent>,
    app: AppHandle,
) {
    const BANNER_COOLDOWN_SECS: u64 = 1800; // 30min entre deux bannières

    // Watcher sur agent_workspace — thread bloquant séparé
    let workspace = PathBuf::from(&settings::load().agent_workspace);
    let tx_ws = tx.clone();
    std::thread::spawn(move || {
        use notify::{Watcher, RecursiveMode, recommended_watcher};
        let (ntx, nrx) = std::sync::mpsc::channel();
        if let Ok(mut w) = recommended_watcher(ntx) {
            let _ = w.watch(&workspace, RecursiveMode::NonRecursive);
            for res in nrx {
                if let Ok(event) = res {
                    let is_code = event.paths.iter().any(|p| {
                        matches!(
                            p.extension().and_then(|e| e.to_str()),
                            Some("md" | "rs" | "py" | "txt")
                        )
                    });
                    if is_code {
                        let _ = tx_ws.blocking_send(PaxEvent::WorkspaceChange);
                    }
                }
            }
        }
    });

    while let Some(_event) = rx.recv().await {
        let now = now_secs();
        if now.saturating_sub(BANNER_LAST_SENT.load(Ordering::Relaxed)) < BANNER_COOLDOWN_SECS {
            continue;
        }

        let s = settings::load();
        let index = embeddings::load_index(&s.embeddings_path);
        let gaps = find_gaps(&s.vault_path, &index);
        if gaps.is_empty() { continue; }

        use tauri::Manager;
        let _permit = {
            let q = app.state::<crate::vram_queue::VramQueue>();
            match q.try_acquire("pax-banner") {
                Some(p) => p,
                None => continue,
            }
        };

        if let Some(question) = evaluate_and_generate(&gaps[0], &s.agents.light_model).await {
            if let Ok(mut last) = LAST_ASKED_NOTE.lock() {
                *last = match &gaps[0] {
                    GapKind::ThinNote { note, .. } => Some(note.clone()),
                    GapKind::SemanticGap { note_a, .. } => Some(note_a.clone()),
                };
            }
            BANNER_LAST_SENT.store(now, Ordering::Relaxed);
            let _ = app.emit("pax-banner", serde_json::json!({ "question": question }));
        }
    }
}
```

- [ ] **Step 5 : Build**

```bash
cd src-tauri && cargo build 2>&1 | grep -E "^error" | head -20
```
Attendu : aucune ligne `error`

- [ ] **Step 6 : Commit**

```bash
git add src-tauri/src/pax_daemon.rs
git commit -m "feat(pax): add PaxEvent channel, PaxEventSender, run_pax_banner() with workspace watcher"
```

---

## Task 4: Wiring dans main.rs

**Files:**
- Modify: `src-tauri/src/main.rs` (~lignes 30, 557, 610-660)

- [ ] **Step 1 : Ajouter l'import mpsc**

Dans les imports en haut de `main.rs`, après `use std::sync::...` :

```rust
use tokio::sync::mpsc;
```

- [ ] **Step 2 : Créer le channel et gérer le sender dans setup()**

Dans la fonction `setup()` (`.setup(|app| {`), avant `let s = settings::load();` :

```rust
let (pax_tx, pax_rx) = mpsc::channel::<pax_daemon::PaxEvent>(16);
app.manage(pax_daemon::PaxEventSender(pax_tx.clone()));
```

Note : `pax_rx` est déclaré ici, sera consommé plus bas dans le bloc `if s.agents.pax_enabled`.

- [ ] **Step 3 : Modifier le spawn pax pour ajouter run_pax_banner et startup trigger**

Trouver le bloc `if s.agents.pax_enabled` (actuellement ~lignes 650-656) et remplacer entièrement par :

```rust
if s.agents.pax_enabled {
    let flag = Arc::new(AtomicBool::new(true));
    let app2 = handle.clone();
    let f2 = flag.clone();
    app.state::<ArmataState>().running_flags.lock().unwrap().insert("pax".into(), flag);
    tauri::async_runtime::spawn(async move { pax_daemon::run_pax(app2, f2).await; });

    let app3 = handle.clone();
    let tx_banner = pax_tx.clone();
    tauri::async_runtime::spawn(async move {
        pax_daemon::run_pax_banner(pax_rx, tx_banner, app3).await;
    });

    // Startup trigger : 45s après init pour laisser l'utilisateur s'installer
    let startup_tx = pax_tx.clone();
    tauri::async_runtime::spawn(async move {
        tokio::time::sleep(std::time::Duration::from_secs(45)).await;
        let _ = startup_tx.send(pax_daemon::PaxEvent::Startup).await;
    });
} else {
    drop(pax_rx); // rx abandonné → try_send depuis Forge échouera silencieusement
}
```

- [ ] **Step 4 : Fixer toggle_agent_daemon pour le cas "pax"**

Dans `toggle_agent_daemon`, dans le `match agent.as_str()` (~ligne 557), avant `_ => return Err(...)` :

```rust
"pax" => {
    tokio::spawn(async move {
        pax_daemon::run_pax(app_clone, flag_clone).await;
    });
}
```

Note : le toggle redémarre uniquement `run_pax` (chemin A). `run_pax_banner` tourne depuis le startup et ne supporte pas le toggle à chaud — comportement suffisant pour l'usage réel.

- [ ] **Step 5 : Build complet**

```bash
cd src-tauri && cargo build 2>&1 | grep -E "^error" | head -20
```
Attendu : aucune ligne `error`

- [ ] **Step 6 : Commit**

```bash
git add src-tauri/src/main.rs
git commit -m "feat(pax): wire PaxEvent channel, startup trigger 45s, fix pax in toggle_agent_daemon"
```

---

## Task 5: ForgeStep triggers dans forge_daemon.rs

**Files:**
- Modify: `src-tauri/src/forge_daemon.rs` (helper + 4 points d'injection)

- [ ] **Step 1 : Ajouter le helper notify_pax()**

Après `fn emit_status(app: &AppHandle, msg: &str)` (actuellement ~ligne 14 de forge_daemon.rs), ajouter :

```rust
fn notify_pax(app: &AppHandle, msg: &str) {
    use tauri::Manager;
    if let Some(sender) = app.try_state::<crate::pax_daemon::PaxEventSender>() {
        let _ = sender.0.try_send(crate::pax_daemon::PaxEvent::ForgeStep {
            message: msg.to_string(),
        });
    }
}
```

`try_send` est non-bloquant — Forge n'attend jamais Pax.

- [ ] **Step 2 : Injection point 1 — distill initial (startup Forge)**

Dans `run_forge()`, trouver (~ligne 270) :

```rust
let learned = crate::memory::distill_vanguard_to_hubs(Some(&app)).await;
if learned > 0 {
    emit_status(&app, &format!("Learned {} facts from Vanguard", learned));
}
```

Remplacer par :

```rust
let learned = crate::memory::distill_vanguard_to_hubs(Some(&app)).await;
if learned > 0 {
    emit_status(&app, &format!("Learned {} facts from Vanguard", learned));
    notify_pax(&app, &format!("Learned {} facts from Vanguard", learned));
}
```

- [ ] **Step 3 : Injection point 2 — refine initial (startup Forge)**

Trouver (~ligne 274) :

```rust
let refined = crate::memory::refine_messy_notes(Some(&app)).await;
if refined > 0 {
    emit_status(&app, &format!("Refined {} messy notes", refined));
}
```

Remplacer par :

```rust
let refined = crate::memory::refine_messy_notes(Some(&app)).await;
if refined > 0 {
    emit_status(&app, &format!("Refined {} messy notes", refined));
    notify_pax(&app, &format!("Refined {} notes", refined));
}
```

- [ ] **Step 4 : Injection point 3 — distill dans la boucle d'événements**

Trouver (~ligne 347) dans la boucle `while running.load(...)` :

```rust
let learned = crate::memory::distill_vanguard_to_hubs(Some(&app)).await;
if learned > 0 {
    emit_status(&app, &format!("Learned {} facts from Vanguard", learned));
}
```

Remplacer par :

```rust
let learned = crate::memory::distill_vanguard_to_hubs(Some(&app)).await;
if learned > 0 {
    emit_status(&app, &format!("Learned {} facts from Vanguard", learned));
    notify_pax(&app, &format!("Learned {} facts from Vanguard", learned));
}
```

- [ ] **Step 5 : Injection point 4 — refine dans la boucle d'événements**

Trouver (~ligne 353) :

```rust
let refined = crate::memory::refine_messy_notes(Some(&app)).await;
if refined > 0 {
    emit_status(&app, &format!("Refined {} notes", refined));
}
```

Remplacer par :

```rust
let refined = crate::memory::refine_messy_notes(Some(&app)).await;
if refined > 0 {
    emit_status(&app, &format!("Refined {} notes", refined));
    notify_pax(&app, &format!("Refined {} notes", refined));
}
```

- [ ] **Step 6 : Build**

```bash
cd src-tauri && cargo build 2>&1 | grep -E "^error" | head -20
```
Attendu : aucune ligne `error`

- [ ] **Step 7 : Commit**

```bash
git add src-tauri/src/forge_daemon.rs
git commit -m "feat(pax): trigger PaxEvent::ForgeStep after distill and refine cycles"
```

---

## Task 6: Bannière UI (#pax-banner)

**Files:**
- Modify: `src/index.html` (ligne 96, après `</div>` du hub-proposal-banner)
- Modify: `src/modules/llm.js` (fin du fichier)

- [ ] **Step 1 : Ajouter le div #pax-banner dans index.html**

À la ligne 96 (après la fermeture du `hub-proposal-banner`), ajouter :

```html
<div id="pax-banner" style="display:none; position:fixed; bottom:80px; left:20px; z-index:9999; background:var(--bg-card,#1e1e2e); border:1px solid rgba(180,140,255,0.4); border-radius:14px; padding:16px 20px; max-width:340px; box-shadow:0 8px 32px rgba(0,0,0,0.5);">
  <div style="font-size:12px; color:rgba(180,140,255,0.9); font-weight:700; margin-bottom:6px;">◈ PAX</div>
  <div id="pax-banner-question" style="font-size:13px; color:rgba(255,255,255,0.85); margin-bottom:12px; line-height:1.5;"></div>
  <div style="display:flex; gap:8px;">
    <button id="pax-banner-open" style="flex:1; background:rgba(180,140,255,0.2); color:#fff; border:1px solid rgba(180,140,255,0.4); padding:8px; border-radius:8px; font-weight:700; cursor:pointer;">Ouvrir le chat</button>
    <button id="pax-banner-dismiss" style="flex:0; background:transparent; color:rgba(255,255,255,0.4); border:none; padding:8px 10px; cursor:pointer; font-size:16px;">✕</button>
  </div>
</div>
```

Note : le banner Forge est en bas-droite (`right:20px`), le banner Pax est en bas-gauche (`left:20px`) pour éviter la superposition.

- [ ] **Step 2 : Ajouter le listener pax-banner dans llm.js**

À la fin de `llm.js` (avant la dernière ligne s'il y en a une, sinon à la suite du dernier bloc), ajouter :

```js
// --- Pax banner (non-chat triggers: startup, forge, workspace) ---
let pendingPaxBannerQuestion = null;

listen('pax-banner', (event) => {
    const q = event.payload.question;
    pendingPaxBannerQuestion = q;
    document.getElementById('pax-banner-question').textContent = q;
    document.getElementById('pax-banner').style.display = 'block';
});

document.getElementById('pax-banner-open')?.addEventListener('click', () => {
    document.getElementById('pax-banner').style.display = 'none';
    if (!pendingPaxBannerQuestion) return;
    switchTab('llm');
    addBubble('ai', pendingPaxBannerQuestion);
    history.scrollTop = history.scrollHeight;
    pendingPaxBannerQuestion = null;
});

document.getElementById('pax-banner-dismiss')?.addEventListener('click', () => {
    document.getElementById('pax-banner').style.display = 'none';
    pendingPaxBannerQuestion = null;
});
```

- [ ] **Step 3 : Build et test manuel**

```bash
cd src-tauri && cargo build 2>&1 | grep -E "^error" | head -5
```

Puis lancer l'app en dev et vérifier :
```bash
source ~/.cargo/env && cargo tauri dev
```

Checklist manuelle :
- [ ] 45s après ouverture : bannière Pax apparaît en bas-gauche avec une question du vault
- [ ] Click "Ouvrir le chat" : switch vers onglet LLM, question injectée comme message AI
- [ ] Click "✕" : bannière disparaît sans effet de bord
- [ ] Interagir dans le chat, puis idle 3min : Pax pose une question directement dans le chat (chemin A)
- [ ] Répondre avec >60 chars : prochain cycle chat-idle arrive plus tôt (~3-4min)
- [ ] Ne pas répondre 15min : prochain cycle arrive plus tard (~20-23min)
- [ ] Forger un cycle Forge (armata tab → "test now") : une bannière Pax émerge dans les 30min suivant le cycle

- [ ] **Step 4 : Commit final**

```bash
git add src/index.html src/modules/llm.js
git commit -m "feat(pax): add pax-banner UI — floating card for non-chat triggers"
```

---

## Tests Rust (récapitulatif)

```bash
cd src-tauri && cargo test -- --test-threads=1 2>&1 | tail -20
```

Tests attendus en plus des tests existants :
- `test_adaptive_cooldown_positive` ✓
- `test_adaptive_cooldown_neutral` ✓
- `test_adaptive_cooldown_ignored` ✓

Les tests sur `LAST_QUALITY` (global mutable) doivent tourner avec `--test-threads=1` pour éviter les interférences entre tests.
